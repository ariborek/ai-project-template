{
  "name": "ai-project-template",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable foundation for safe AI-assisted software projects.",
  "type": "module",
  "engines": {
    "node": ">=24.18.0"
  },
  "scripts": {
    "validate": "node scripts/validate-template.mjs",
    "test": "node --test scripts/validate-template.test.mjs",
    "check": "npm run validate && npm test"
  },
  "templateValidation": {
    "mode": "template"
  }
}
